"use strict";

/**
 * Returns the length of the longest absolute path to a file in the abstracted file system
 * @param fileSystem file system as string
 */
function lengthOfLongestAbsolutePathToFileInFileSystem(fileSystem) {
  // Your code here
}

module.exports = lengthOfLongestAbsolutePathToFileInFileSystem;
