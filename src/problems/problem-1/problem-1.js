"use strict";

/**
 * Returns wether two elements in the array adds up to k
 * @param array the array of numbers
 * @param k the target sum
 */
function twoElementsAddUpTo(array, k) {
  // Your code here
}

module.exports = twoElementsAddUpTo;
