"use strict";

/**
 * Returns the minimum cost ensuring that no two neighboring houses are of the same color
 * @param matrix N * K matrix of costs
 */
function computeBuilderMinimumCost(matrix) {
  // Your code here
}

module.exports = computeBuilderMinimumCost;
