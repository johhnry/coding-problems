"use strict";

/**
 * Finds the minimum number of rooms required
 * @param intervals array of intervals (two element lists)
 */
function minimumNumberOfRoomsRequired(intervals) {
  // Your code here
}

module.exports = minimumNumberOfRoomsRequired;
